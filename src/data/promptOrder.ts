/**
 * Stable Diffusion プロンプト カテゴリ出力順序
 *
 * SD/Pony/Illustrious のベストプラクティスに基づく順序:
 *   1. 被写体   — 誰を・何を描くかをモデルに最初に伝える
 *   2. 外見     — 服装・髪・目など視覚的詳細（重要度順）
 *   3. 表情・状態 — 感情・物理エフェクト
 *   4. 動作・シーン — ポーズ・状況・R18
 *   5. 構図・背景 — 画面設計・ロケーション
 *   6. スタイル  — 画風・色調は最後に全体を調整
 *
 * ※ チェックポイントの qualityPrompt は全カテゴリより先頭に付与される
 */
export const PROMPT_CATEGORY_ORDER: readonly string[] = [
  // ── 被写体 ────────────────────────────────────────
  "quality",             // クオリティ（ユーザー選択の品質タグ）
  "count",               // 人数・性別
  "character_type",      // キャラタイプ・種族
  "subject_female_age",  // 女性・年齢
  "subject_female_body", // 女性・体型
  "subject_male_age",    // 男性・年齢
  "subject_male_body",   // 男性・体型
  "subject_other",       // 被写体・その他

  // ── 外見 ──────────────────────────────────────────
  "outfit",              // 服装（最も視覚的に支配的なため先）
  "hairstyle",           // 髪型
  "hair_color",          // 髪色
  "eye_features",        // 目・瞳
  "accessories",         // アクセサリー
  "job_class",           // 職業・クラス（装備・コスチューム含む）

  // ── 表情・状態 ────────────────────────────────────
  "expression",          // 表情
  "state_effects",       // 状態・エフェクト（汗・涙など）

  // ── 動作・シーン ──────────────────────────────────
  "action",              // 動作・ポーズ
  "situation",           // シチュエーション
  "summoning",           // 召喚・使い魔
  "r18_general",         // R18・汎用
  "r18_male",            // R18・男性向け

  // ── 構図・背景 ────────────────────────────────────
  "composition",         // 構図・カメラ
  "focus",               // フォーカス部位
  "background",          // 背景・場所
  "lighting",            // ライティング

  // ── スタイル ──────────────────────────────────────
  "style",               // スタイル・画風
  "color",               // カラー・トーン
] as const;
